#include<stdio.h>
int main()
{
    int j,k;
    int a[j][k],b[j][k],c[j][k];
    printf("Enter the elements of first matrix ");
    for (int j = 0; j < 3; j++)
    {
        for (int k = 0; k < 3; k++)
        {
       scanf("%d", a[j][k]);
            
        }
        
    }
    
return 0;
}